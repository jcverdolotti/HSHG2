<header>
<div class="navbar navbar-dark  navbar-expand flex-colum flex-md-row bd-navbar" width="100%" role="navigation">
          <!-- Image and text --> 
      <nav class="navbar navbar-light bg-dark navbar-collapse fixed-top" >
              
                <%= image_tag("logo.svg",size: "60x60", class:"d-inline-block align-top")%>
                <a class="navbar-brand mr-auto" href="/">Home Switch Home </a>
              
              <% if signed_in? %>
                <% if current_user.user_type == 1 || current_user.user_type == 4%>
                  <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">     
                      <li class="nav-item">
                        <%= link_to("Inscripciones a subastas", apetitions_path, class:"nav-link") %>
                      </li>
                    </ul>
                    <ul class="navbar-nav">
                      <div class="dropdown" aria-labelledby="navbarDropdown">
                        <li>
                          <button class="btn nav-link btn-outline-dark  dropdown-toggle" id="dropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                          Listados</button>
                          <div class="dropdown-menu dropdown-menu-right">
                            <%= link_to "Listado de residencias", residences_path, class:"dropdown-item"%>
                            <%= link_to "Listado de usuarios", vistas_path, class:"dropdown-item"%>
                            <%= link_to "Listado de Administradores", veradmin_path, class:"dropdown-item"%>
                            <%= link_to "Listado de peticiones", petitions_path, class:"dropdown-item"%>
                          </div>
                        </li>
                      </div>
                    </ul>
                  </div>
                
                    <% end %>
                    <% end %>
                    
                  <div >
                    <% if signed_in? %>
                      <% if current_user.user_type == 2 || current_user.user_type == 3%>
                        <div class="collapse navbar-collapse" id="navbarNav">
                          <ul class="navbar-nav">     
                            <li class="nav-item"><%= link_to("Ver todas las residencias", residences_path, class:"nav-link")%>
                            <span class="sr-only">(current)</span>      
                            </li>
                            <li class="nav-item"><%= link_to("Ver hotsales", hotsales_path, class:"nav-link")%>
                            </li>
                          </ul>
                        </div>
                      <%end%>
                    <%end%>
                  </div>

                      <div>
                      <% if signed_in? %>
                      <div class="dropdown">
                        <button class="btn btn-outline-light dropdown-toggle" type="button" id="dropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Usuario</button>
                        <div class="dropdown-menu dropdown-menu-right">
                          <%= link_to "Ver perfil", vista_path(current_user), class: "dropdown-item"%>
                          <%=link_to "Editar Perfil", edit_user_registration_path, class:"dropdown-item"%>
                          <%= link_to "Mis Reservas", misreservas_path, class:"dropdown-item"%>
                          <%=link_to "Cambiar membresía", new_petition_path, class:"dropdown-item"%>
                          <%= link_to "Salir", destroy_user_session_path, method: :delete, class:"dropdown-item"%>
                        </div>
                      </div>
                    <% else %>
                      <div class="btn-group" role="group" aria-label="Basic example">
                        <%= link_to "Registrarse", new_user_registration_path,class:"btn btn-outline-light" %>
                        <%= link_to "Ingresar", new_user_session_path,class:"btn btn-outline-light" %>
                        </div>
                    <% end %>
                  </div>        
      </nav>
      
</div>
</header>